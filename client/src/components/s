 <input
   placeholder="room..."
   onChange={(e) => {
     setRoom(e.target.value);
   }}
 />
 <button onClick={joinRoom}>send</button>
 <input
   placeholder="message..."
   onChange={(e) => {
     setMessage(e.target.value);
   }}
 />
 <button onClick={sendMessage}>send</button>
 <button onClick={sendMessageAndRoom}>send room</button>
 <button onClick={sendMessageAndRoom}>send room</button>
 <h1>Message:</h1>
 {messageReceive}